# 🚀 ІНСТРУКЦІЯ: Синхронізація дат закриття актів

## ⭐ РЕКОМЕНДОВАНИЙ СПОСІБ: SQL-запит в Supabase

### Крок 1: Відкрийте Supabase Dashboard
1. Перейдіть на https://supabase.com
2. Виберіть ваш проект `shlifservice24`
3. В лівому меню натисніть **SQL Editor**

### Крок 2: Відкрийте файл sync_dates.sql
1. Знайдіть файл `sync_dates.sql` в корені проекту
2. Відкрийте його в редакторі
3. Скопіюйте **весь** код

### Крок 3: Запустіть SQL-скрипт
1. Вставте код в SQL Editor в Supabase
2. Натисніть **Run** (або `Ctrl+Enter`)
3. Дочекайтеся завершення

### Крок 4: Перевірте результат
В консолі Supabase ви побачите:
```
🚀 Початок синхронізації дат закриття актів...
============================================================
🔄 Міша Шліф (Слюсар): Акт 111 - "null" → "2026-02-17"
✅ Міша Шліф: оновлено 1 актів
============================================================
📊 ПІДСУМОК СИНХРОНІЗАЦІЇ:
============================================================
👥 Оброблено користувачів: 8
✅ Оновлено користувачів: 1
📋 Оновлено актів: 1
============================================================
✅ Синхронізація завершена успішно!
```

---

## 📋 Що робить SQL-скрипт?

1. ✅ Проходить по всіх записах в таблиці `slyusars`
2. ✅ Для кожного користувача читає поле `data->Історія`
3. ✅ Знаходить всі акти з `"ДатаЗакриття": null`
4. ✅ Бере правильну дату з таблиці `acts` (поле `date_off`)
5. ✅ Конвертує в формат `YYYY-MM-DD`
6. ✅ Оновлює JSON в стовпці `data`
7. ✅ Показує детальний звіт

---

## ⚠️ Важливо!

- ✅ Скрипт **безпечний** - тільки оновлює дати, не видаляє дані
- ✅ Працює для **ВСІХ** користувачів (слюсарів, приймальників)
- ✅ Оновлює тільки акти з `null` або невірною датою
- ⚠️ Рекомендується зробити резервну копію перед запуском

---

## 🔍 Приклад оновлення

**До:**
```json
{
  "Акт": "111",
  "ДатаЗакриття": null,
  "Записи": [...]
}
```

**Після:**
```json
{
  "Акт": "111",
  "ДатаЗакриття": "2026-02-17",
  "Записи": [...]
}
```

---

## ✅ Готово!

Після запуску SQL-скрипта всі дати будуть синхронізовані, і проблема з `NaN.NaN.NaN` більше не виникатиме!
